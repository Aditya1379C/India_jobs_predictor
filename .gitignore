# Environment — never commit real API keys
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Data & models (large / generated locally)
# Note: the daily-retrain GitHub Actions workflow force-adds
# report/dashboard.html + models/*.pkl + models/*.json (git add -f),
# so those specific files do get committed despite being listed here.
data/jobs.db
models/*.pkl
models/*.json
report/

# Logs
logs/
*.log

# macOS
.DS_Store

# Local-only docs
PROJECT_BREAKDOWN.md
